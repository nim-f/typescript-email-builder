export enum BLOCK_TYPES {
    Block = 'block',
    Row = 'row',
}

export const enum BLOCK_NAMES {
    logo = 'logo',
    text = 'text',
    row = 'row',
}

export const INSTRUMENTS: Array<BLOCK_NAMES> = [
    BLOCK_NAMES.logo,
    BLOCK_NAMES.text,
]
